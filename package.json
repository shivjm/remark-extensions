{
  "engines": {
    "node": ">=10",
    "pnpm": ">=3"
  },
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "lint": "pnpm lint -r",
    "build": "pnpm build -r --parallel",
    "test": "pnpm test -r"
  }
}
